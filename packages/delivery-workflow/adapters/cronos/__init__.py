# adapters/cronos — Cronos-specific ExecutorInterface adapter (deferred to Phase 6)
# NOTE: this adapter IS allowed to import app.* (it is the portability seam).
# The import-linter contract in .importlinter excludes this subtree.
