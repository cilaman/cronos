---
agent_mode: auto
agent_model: default
claude_session_id: 93ec1352-d9de-47b7-8bfc-e543484f7b45
created_at: '2026-05-27T13:09:36Z'
depends_on: []
id: 2026-05-27-1309-test-health
manual_order: 0
parent_id: null
pending_messages: []
pr_url: null
priority: 3
proposed_pr_path: null
space_id: cronos-development
state: archived
title: TEST HEALTH
type: task
updated_at: '2026-06-03T14:12:50Z'
waiting_question: null
---

# Brief

TEST HEALTH in Dashboard shows only the information about runs, but not the details nor test coverage.

# History

```
2026-05-27T13:19:51Z [agent] run=0 model=claude-sonnet-4-6 mode=auto agents=explore
Let me explore the codebase to understand the current TEST HEALTH dashboard implementation.

Now let me read the relevant files to understand exactly what changes are needed.

Now let me read the TEST HEALTH section in DashboardPage.tsx and the types.

Let me read the TEST HEALTH section of DashboardPage.tsx.

Let me also look at TestReportsPage.tsx to see how it renders suite details and coverage.

Now I have everything I need. Let me implement the fix — I'll add suite breakdown and coverage details to the dashboard's TEST HEALTH section.

Now I'll implement the fix. I'll update `DashboardPage.tsx` to fetch the full latest test report and display suite details and per-module coverage.

Now add the suite/coverage helper components right after `TrendStrip`:

Now add the `useLatestTestReport` hook call in the component and update the TEST HEALTH section to show details:

Now update the TEST HEALTH section to render `TestDetails`:

Now let me verify the TypeScript compiles correctly by running the frontend build check:

No TypeScript errors. Let me do a quick sanity check on the changed section to confirm everything looks right:

The implementation looks correct. The fix adds:

1. **`useLatestTestReport` hook call** in the dashboard to fetch the full `TestReport` (with `suites` and `coverage_data`) alongside the existing summaries list.

2. **`SuiteRow` + `TestCaseRow`** — collapsible suite rows that expand to show individual test cases with status badges, duration, and error message details for failures.

3. **`CoverageBar`** — per-module coverage bars with color coding (red <40%, amber <70%, green ≥70%).

4. **`TestDetails` component** — composes the above two sections and renders below the existing trend strip when a full report is available.

The dashboard's TEST HEALTH section now shows: aggregate counts → run history trend → test suite breakdown (expandable) → per-module coverage breakdown.
```

```
2026-05-27T13:20:28Z [user]
commit to main, push
```

```
2026-05-27T13:22:22Z [agent] run=1 model=claude-sonnet-4-6 mode=auto
Main is checked out in the primary worktree. I'll commit and push there directly.

The workspace and main worktree are separate. I need to copy the modified file into the main worktree first.

The main worktree has some styling changes that diverged from the workspace base. Let me apply only the test health changes cleanly using a patch.

The conflict is just a section comment difference. I'll resolve it by keeping the new components and using main's comment.

Now verify no more conflicts and TypeScript is clean, then commit and push:

(stopped by user)
```
