---
agent_mode: auto
agent_model: opus-4-8
claude_session_id: 1c858d20-92d8-4d6d-893a-f26b64c5d795
created_at: '2026-06-06T09:37:31Z'
depends_on: []
feature_key: null
feature_state: null
id: 2026-06-06-0937-features-eror
issue_number: null
issue_url: null
manual_order: 0
parent_id: null
pending_messages: []
pr_url: null
priority: 3
proposed_issue_path: null
proposed_pr_path: null
realizes: null
space_id: cronos-development
state: archived
title: Features eror
type: task
updated_at: '2026-06-13T10:30:16Z'
waiting_question: null
---

# Brief

Features page returns error Error: 404 on /api/spaces/cronos-development/features: {"detail":"Not Found"}

# History

```
2026-06-06T09:40:25Z [agent] run=0 model=claude-opus-4-8 mode=auto
I'll investigate the 404 error on the features endpoint. Let me look at the backend API.

The backend uses `/api/features?space_id=` but the frontend is calling `/api/spaces/{space_id}/features`. Let me check the frontend API client.

There's a mismatch: frontend calls `/api/spaces/{spaceId}/features` but backend exposes `/api/features?space_id=`. Let me examine the backend routes and schemas in detail.

Now I understand the mismatch. The frontend `api.ts` calls don't match the backend contract. Let me check how these functions are consumed in the frontend.

Let me read the hooks and the create flow to understand expected shapes.

Let me check the backend models for exact field names.

The backend is the source of truth (tested). I need to fix the frontend `api.ts` to match. Let me check whether any frontend tests assert the actual request URLs.

No URL assertions in tests. Let me check what FeaturesBoard passes to createFeature.

All clear. The fix is to align `api.ts` with the backend contract. Let me update the three functions.

Now let me verify the backend router is actually registered in main.py, to confirm the endpoint exists.

The backend route is registered. Let me run the frontend feature tests and typecheck to confirm nothing breaks.

I'm already in `frontend`. Let me run the checks.

Dependencies aren't installed in this worktree. Let me install them.

Tests pass (24/24) and the build succeeds. The fix is complete.

The root cause: the frontend `api.ts` was calling endpoints that don't exist on the backend. The backend `features_router` (mounted at `/api/features`) expects:
- `GET /api/features?space_id=` — not `/api/spaces/{id}/features`
- `PATCH /api/features/{id}/feature-state` with `{feature_state}` — not `/api/tasks/{id}/feature-state` with `{state}`
- `POST /api/features` with `{space_id, title, type, brief}` — not `/api/spaces/{id}/features` with `{description}`

I aligned all three `api.ts` functions (`features`, `transitionFeatureState`, `createFeature`) to the backend contract. The hook layer and components are untouched, so no other call sites needed changes.

Let me finalize the task.

Task is complete. Let me handle git. First detect task context.

My code change is in the workspace worktree, not the space dir. Let me check the worktree's git status (the space dir only shows unrelated runtime `.cronos` state).

My change is isolated here on the task branch. Let me commit and push it.

Pushed. The fix is committed and verified.

Fixed the Features page 404 by aligning the frontend API client (`frontend/src/api.ts`) with the backend `/api/features` contract — the client was calling non-existent `/api/spaces/{id}/features` routes.

MEMORY[fact]: Features page 404 root cause was a frontend/backend route mismatch in `frontend/src/api.ts`. Backend `features_router` (backend/app/api/features.py) is mounted at `/api/features`: `GET /api/features?space_id=`, `PATCH /api/features/{id}/feature-state` with body `{feature_state}`, `POST /api/features` with body `{space_id,title,type,brief}`. The frontend was calling `/api/spaces/{id}/features` and `/api/tasks/{id}/feature-state`. Fixed all three `api.ts` functions (`features`, `transitionFeatureState`, `createFeature`); hooks/components unchanged. 24 feature tests + build pass.

STATUS: DONE
```

```
2026-06-06T09:42:05Z [user]
Is the fix in main? Problem still persists after upgrade
```

```
2026-06-06T09:43:21Z [agent] run=1 model=claude-opus-4-8 mode=auto
Confirmed — the fix is on the task branch only; main still has the buggy URLs, so the upgrade (which deploys from main) didn't pick it up. Let me merge it into main. First, where is main checked out?

The space dir is on a feature branch; main isn't checked out anywhere. My commit `8d47ccc` sits directly on top of main's HEAD (`d6b7a98`), so this is a clean fast-forward. Let me verify and push to main.

Clean fast-forward confirmed. Pushing the fix to main.

The push to **origin/main** succeeded (`d6b7a98..8d47ccc`). The local main ref couldn't be force-updated because it's checked out in another worktree, but that's irrelevant — deployment pulls from origin/main, which now has the fix.

The reason the problem persisted: my earlier finalize only pushed to the task branch, not main, so the previous upgrade deployed code that still had the old URLs. Now that the fix is in origin/main, re-upgrading will deploy it.

Per the project workflow (commit to main → push → upgrade), I'll trigger the upgrade now to deploy the fix.

STATUS: DONE

The fix is now in **origin/main** (`8d47ccc`) and the upgrade has started to deploy it.

**Why the problem persisted:** my earlier finalize pushed the fix only to the task branch `cronos/2026-06-06-0937-features-eror`, not to main. The upgrade deploys from main, which still had the old `/api/spaces/{id}/features` URLs — so nothing changed for you.

**Now fixed:** verified main only had the buggy URLs, fast-forwarded the fix commit onto origin/main, and triggered the upgrade. Once the container finishes restarting, the Features page should load instead of returning 404.

I'll save a note so this doesn't recur — standalone task fixes must reach main, not just the task branch, for upgrades to pick them up.

Done. Summary:

- **Root cause of persistence:** the fix had only been pushed to the task branch, not main. Upgrades deploy from origin/main, which still had the buggy URLs.
- **Resolved:** fast-forwarded the fix commit `8d47ccc` onto **origin/main** and triggered the upgrade. Once the container restart completes, the Features page will load instead of 404'ing.
- Saved a memory note so future standalone fixes get pushed to main before upgrading.
```

```
2026-06-06T09:44:59Z [user]
Now the error is Error load failed
```

```
2026-06-06T09:48:47Z [agent] run=2 model=claude-opus-4-8 mode=auto
The 404 is gone (URL is correct now), but there's a new error. "Load failed" is WebKit's generic fetch-failure message. Let me hit the actual endpoint to see what the backend returns now.

HTTP 307 — the endpoint is defined as `@router.get("/")` so the real path is `/api/features/` (trailing slash), and FastAPI redirects `/api/features` → `/api/features/`. Let me check the redirect target.

The redirect points to the **internal Docker hostname** `http://backend:8000/...`, which the browser can't reach — so following the 307 fails with "Load failed". The root cause is the route is `@router.get("/")` (trailing slash) while the frontend requests `/api/features` (no slash). Let me check the convention other routers use.

Confirmed: tasks.py and spaces.py use `@router.get("")`/`@router.post("")` (no trailing slash) for collection roots. features.py uses `"/"`, which triggers the 307 redirect to the unreachable internal hostname. Let me fix the two root routes to match the convention.

Now let me check whether any backend tests rely on the trailing-slash path.

Tests POST to `/api/features/` (trailing slash); with `redirect_slashes` (default on) the TestClient will follow the 307 to `/api/features`, so they should still pass. Let me run the feature tests to confirm.

Only one test failed — the others followed the redirect fine. Let me look at the failing test.

This test uses raw `httpx.AsyncClient` which doesn't follow redirects, so it correctly catches the trailing-slash behavior. The convention-aligned fix is route=`""` (matching tasks.py/spaces.py); I'll update the tests that hardcode the trailing-slash root path. Let me update them.

There's also `test_features_e2e.py`. Let me fix all the `/api/features/?space_id=` occurrences too.

This comment documents the exact bug. Let me update the comment and the three GET calls in the e2e test.

Now let me run both feature test files.

All 27 pass. Let me scan the whole test suite for any remaining bare-root trailing-slash references I may have missed.

The remaining matches are docstrings/comments or `/{id}` subpaths. Let me check the router registration test — it may assert on the root route paths.

This test already uses the no-slash form — it'll now pass directly. Let me run the full feature-related test set to confirm everything is green.

All 41 pass. Let me commit, then fast-forward to main and upgrade.

Both `main` and the task branch are updated. Let me record the pitfall in memory, then upgrade.

Both `origin/main` and the task branch now have the fix (commit `51b8fba`). Triggering the upgrade to deploy it.

**Root cause of "Load failed":** The features collection routes were declared `@router.get("/")`/`post("/")`, so the canonical path was `/api/features/`. A browser request to `/api/features` got a 307 redirect whose `Location` was the absolute internal Docker URL `http://backend:8000/api/features/...` — unreachable from the browser, surfacing as WebKit's "Load failed". I aligned the routes with the tasks/spaces convention (`@router.get("")`), updated the tests that hardcoded the trailing slash, and verified all 41 feature tests pass.

STATUS: DONE

The fix is in `origin/main` (`51b8fba`) and the upgrade has started.

Summary of this round:
- **Why it changed from 404 to "Load failed":** the first fix corrected the URL path, so the request finally reached the backend — which then 307-redirected `/api/features` to the unreachable internal hostname `http://backend:8000/...`.
- **Fix:** changed the two collection routes in `backend/app/api/features.py` from `@router.get("/")`/`post("/")` to the no-trailing-slash form (`""`), matching `tasks.py`/`spaces.py`. Updated the feature tests that hardcoded the slash; all 41 pass.
- Pushed to both main and the task branch, recorded the pitfall in memory, and triggered the upgrade. Once the container restart completes, the Features page should load.
```
