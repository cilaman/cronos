---
agent_mode: auto
agent_model: sonnet
claude_session_id: f311082e-8a56-42c3-a5e3-6d442096793a
created_at: '2026-05-30T14:37:55Z'
depends_on:
- 2026-05-30-1437-4-4-auto-improvement-applier
id: 2026-05-30-1437-4-5-evals-ci-gate
manual_order: 0
parent_id: 2026-05-30-1437-self-improvement-loop-retro-known-issues
pending_messages: []
pr_url: null
priority: 2
proposed_pr_path: null
space_id: cronos-development
state: done
title: 4.5 evals + CI gate
type: task
updated_at: '2026-05-30T23:26:04Z'
waiting_question: null
---

# Brief

Port Delivery Notes `run_evals.py`. Wire the golden+negative fixtures into the `goal-finalize` test gate so ANY change to the contract, schemas, normalizer, or agents must pass the fixtures before merge.

## Acceptance
- A regressing change is blocked by the eval gate during goal-finalize.

## Reference material (read by absolute path)
- Delivery Notes contract: `/data/spaces/delivery-notes/.ai/framework/agent-contract-v1.md`
- Delivery Notes orchestrator: `/data/spaces/delivery-notes/.claude/agents/pipeline-orchestrator.md`
- Delivery Notes known-issues (F-NN): `/data/spaces/delivery-notes/.ai/framework/known-issues.md`
- Cronos primitives: `backend/app/{agent,worker,goal_sync,storage,models,trace_store,trace_parser,memory_store,memory_retrieval}.py`
- Memory entries `delivery-notes-pipeline` and `delivery-notes-pipeline-cronos-mapping` surface the design rationale via retrieval.

# History

```
2026-05-30T23:26:04Z [agent]
I'll start by exploring the reference material and existing Cronos pipeline infrastructure to understand what needs to be ported.

Now let me read the actual run_evals.py file and look at the existing Cronos pipeline infrastructure:

Let me read the existing fixtures and test files to understand what's already in place:

Let me read the verify.py to understand its API:

Now I have everything I need. Let me implement the eval harness and update `goal-finalize`:

Good. Now I'll create the standalone `run_evals.py`:

Now let's smoke-test it:

All 21 fixtures pass. Let me verify the exit code and JSON mode:

Now let me verify a regression is actually blocked. I'll test a scenario where a golden fixture regresses:

A regression is correctly detected. Now update `goal-finalize` to add the eval gate:

The skill updated correctly. Now run the full test suite to confirm the new module doesn't break anything:

The 23 tests pass but coverage drops because `run_evals.py` has 0% coverage. I need to add tests. Let me check the full suite first:

80% coverage - above the floor. Now I'll add tests for the new `run_evals.py` to bring it above 0%:

93% coverage on `run_evals.py`, 20 new tests pass. Let me run the full suite:

1249 tests pass, 83% coverage (up from 80% before adding tests). Now let me commit and push:

Pushed. Now finalize the task:

## Step 1 — Verify completion

All requirements met:
- `backend/app/pipeline/run_evals.py` created — standalone eval harness porting Delivery Notes' `run_evals.py`, adapted for Cronos classes/paths
- 20 new tests in `backend/tests/test_pipeline_run_evals.py` (93% coverage on new file)
- `goal-finalize/SKILL.md` updated — Step 3 now runs `python -m app.pipeline.run_evals --all` before the pytest suite; any golden regression or negative passing through exits 1 and blocks merge
- Verified: mutating a golden fixture causes `main()` to return 1

Full suite: 1249 passed, 83% coverage.

## Step 3 — Git

Already handled via `/goal-task-commit` above — commit `98acd9c` pushed to `feature/self-improvement-loop-retro-known-issues`.

## Step 4 — Write memory

Now also write the project-level memory to the space-root so it's visible in future conversations:

Memory written. Now mark the task done via the API:

The state machine transition from `active → done` is handled by the Cronos worker reading `STATUS: DONE` from my output — not via direct API call. Memory is written. All done.

MEMORY[fact]: `backend/app/pipeline/run_evals.py` — standalone CC-v1 eval harness (Cronos port of Delivery Notes `run_evals.py`). Auto-discovers golden + negative fixtures from `backend/app/pipeline/fixtures/`. Exit 0=pass, 1=regression. 20 tests in `test_pipeline_run_evals.py` (93% coverage). `goal-finalize` Step 3 now runs this before pytest.

STATUS: DONE
```
