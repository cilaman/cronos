---
agent_mode: auto
agent_model: sonnet
claude_session_id: a39e323c-51e4-4a9f-9178-13021121d37c
created_at: '2026-05-30T14:37:55Z'
depends_on:
- 2026-05-30-1437-1-3-verifier
id: 2026-05-30-1437-1-4-normalizer
manual_order: 0
parent_id: 2026-05-30-1437-pipeline-foundation-cc-v1-contract-schem
pending_messages: []
pr_url: null
priority: 2
proposed_pr_path: null
space_id: cronos-development
state: done
title: 1.4 Normalizer
type: task
updated_at: '2026-05-30T15:40:26Z'
waiting_question: null
---

# Brief

Build `backend/app/pipeline/normalize.py` implementing the drift-absorption table (string blocker -> {description,severity:medium}; backslash->slash in paths; Title-Case section rename -> canonical; status=partial+blockers -> blocked; strategy synonym mapping). Never invent content. Wire `verify.py --normalize` to run normalize-then-verify. Mirror Delivery Notes `normalize_outputs.py`.

## Acceptance
- `--dry-run`/`--json` supported.
- Known drifts auto-fixed; missing required fields/sections still hard-fail.

## Reference material (read by absolute path)
- Delivery Notes contract: `/data/spaces/delivery-notes/.ai/framework/agent-contract-v1.md`
- Delivery Notes orchestrator: `/data/spaces/delivery-notes/.claude/agents/pipeline-orchestrator.md`
- Delivery Notes known-issues (F-NN): `/data/spaces/delivery-notes/.ai/framework/known-issues.md`
- Cronos primitives: `backend/app/{agent,worker,goal_sync,storage,models,trace_store,trace_parser,memory_store,memory_retrieval}.py`
- Memory entries `delivery-notes-pipeline` and `delivery-notes-pipeline-cronos-mapping` surface the design rationale via retrieval.

# History

```
2026-05-30T15:40:26Z [agent]
I'll start by reading the reference materials to understand what needs to be built.

Now let me read the Delivery Notes normalize_outputs.py and the existing Cronos verifier.

Let me find the actual backend directory structure.

The pipeline directory doesn't exist. Let me check the git branch to see what's already there.

The feature branch has prior tasks. I need to rebase onto it to get the pipeline directory. Let me check out that branch.

Now let me read the existing verify.py and contract.py to understand what I need to integrate with.

Now let me look at the existing `__init__.py` and schemas directory.

Now let me check existing pipeline tests for context.

I have all the context I need. Now I'll write `backend/app/pipeline/normalize.py`.

Now update `__init__.py` to export the normalizer's public symbols:

Now let me run the existing tests to confirm nothing breaks, and add tests for the normalizer:

One test is failing because the placeholder behavior (normalizer not available) is now wired. Let me look at it:

This test was a placeholder for "task 1.4 not yet landed." I need to replace it with a test that validates the normalizer works through the `--normalize` flag.

Now let's write a dedicated test file for the normalizer:

All 109 tests pass. Let me check the coverage config to understand what's measured.

All 1121 tests pass with 82% total coverage. Now let me commit and push to the feature branch.

I'm on the task branch, not the feature branch. Let me check what happened with the rebase.

The feature branch is in another worktree. I'll copy changes there and commit via `git -C`.

Pushed. Now finalize the task.

## Step 1 — Verify completion

All task requirements met:
- `backend/app/pipeline/normalize.py` built with all six drift fixers from the auto-fix table
- `--dry-run` and `--json` flags supported
- Known drifts auto-fixed; missing required fields/sections remain hard-fails (no invented content)
- `verify.py --normalize` now works (was pre-wired in task 1.3, now the import resolves)
- Mirrored Delivery Notes `normalize_outputs.py` adapted for Cronos (`memory_retrieval` replaces `kb_search`)
- 1121 tests pass, 82% coverage, committed and pushed to feature branch

## Step 3 — Handle git

Already done via `goal-task-commit` — committed as "1.4 Normalizer" and pushed to `feature/pipeline-foundation-cc-v1-contract-schem`.

## Step 4 — Write memory

MEMORY[fact]: `backend/app/pipeline/normalize.py` landed (task 1.4). Six drift fixers: blocker_string_wrap, strategy_synonym (kb_search→memory_retrieval), strategy_unknown_drop, section_case_canonical, status_partial_to_blocked, path_backslash_to_slash. `normalize(agent, slug, space, dry_run) → NormalizeResult` — same API shape as Delivery Notes. verify.py --normalize now fully functional. Tests in `backend/tests/test_pipeline_normalize.py` (35 tests).

STATUS: DONE
```
