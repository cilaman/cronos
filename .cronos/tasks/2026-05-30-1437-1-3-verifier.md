---
agent_mode: auto
agent_model: opus
claude_session_id: c90488f0-e740-478a-b525-2a05e2a15c25
created_at: '2026-05-30T14:37:55Z'
depends_on:
- 2026-05-30-1437-1-2-per-class-artifact-schemas
feature_key: null
feature_state: null
id: 2026-05-30-1437-1-3-verifier
issue_number: null
issue_url: null
manual_order: 0
parent_id: 2026-05-30-1437-pipeline-foundation-cc-v1-contract-schem
pending_messages: []
pr_url: null
priority: 2
proposed_issue_path: null
proposed_pr_path: null
realizes: null
space_id: cronos-development
state: archived
title: 1.3 Verifier
type: task
updated_at: '2026-06-19T12:17:47Z'
waiting_question: null
---

# Brief

Build `backend/app/pipeline/verify.py` + CLI (`--agent --slug --space [--normalize] [--json]`). Validate an artifact against its class schema + cross-field rules R1-R7 (R1 non-empty blockers => status in {blocked,failed}; R2 status=done => confidence>=0.7; R3 confidence in [0,1]; R4 files_read+memory_hits>=len(inputs_used); R5 outputs_produced[0]==canonical path; R6 slug==goal slug; R7 workspace-relative forward-slash paths) plus per-class extensions (R-impl/R-val/R-rev/R-doc). Exit codes: 0 proceed, 1 fail, 2 escalate, 3 retry. Mirror Delivery Notes `verify_outputs.py` semantics.

## Acceptance
- CLI returns correct exit code per gate vocabulary.
- All R1-R7 + per-class rules enforced with clear error messages.

## Reference material (read by absolute path)
- Delivery Notes contract: `/data/spaces/delivery-notes/.ai/framework/agent-contract-v1.md`
- Delivery Notes orchestrator: `/data/spaces/delivery-notes/.claude/agents/pipeline-orchestrator.md`
- Delivery Notes known-issues (F-NN): `/data/spaces/delivery-notes/.ai/framework/known-issues.md`
- Cronos primitives: `backend/app/{agent,worker,goal_sync,storage,models,trace_store,trace_parser,memory_store,memory_retrieval}.py`
- Memory entries `delivery-notes-pipeline` and `delivery-notes-pipeline-cronos-mapping` surface the design rationale via retrieval.

# History

```
2026-05-30T15:31:22Z [agent]
Need to base my work on the feature branch which has the schemas from task 1.2. Let me rebase.

Now let me check the Delivery Notes verify_outputs.py for reference semantics.

Now I have enough context to design the verifier. Let me write it.

Now let's expose verify from `__init__.py` and run a basic smoke test.

Now let me write the tests.

All 76 tests pass. Let me fix the warning — pytest is interpreting `test_header` (helper builder) as a test.

All 75 tests pass with no warnings. Let me also verify the full test suite still passes and coverage is OK.

All 1087 tests pass; coverage at 82% (verify.py: 83%). Coverage floor met. Let me commit and finalize.

Now fast-forward the feature branch to my new commit and push.

Pushed successfully. Now let me update the local feature branch ref by going through the other worktree.

Feature branch fast-forwarded both locally and remotely. Now let me finalize.

Task is already complete: all R1-R7 + per-class rules implemented, 75 verifier tests pass, full suite 1087 passes at 82% coverage, commit pushed to feature branch.

Let me write memory.

Built `backend/app/pipeline/verify.py` (~750 LOC): class-keyed verifier with R1-R7 base rules + per-class extensions (R-impl-1..6, R-val-1..4, R-rev-1..6, R-doc-1..5), schema-driven base header checks, section presence with alias support (Findings/Decisions/Top relevance, Open questions/Blockers), and CLI with the 0/1/2/3 (proceed/fail/escalate/retry) exit-code vocabulary. Mirrors Delivery Notes' `verify_outputs.py` semantics adapted to Cronos (`memory_hits` instead of `kb_hits`, trace-owned metrics rejected, artifacts under `.cronos/pipeline/`). 75 verifier tests + 1087 total, 82% project coverage. Committed and pushed to `feature/pipeline-foundation-cc-v1-contract-schem`.

MEMORY[fact]: CC-v1 verifier shipped at `backend/app/pipeline/verify.py`. Public surface: `verify(agent_class, slug, space) -> VerifyResult`, `CLASS_CONFIG` (research/analysis/design/implementation/test/review/doc), exit codes `EXIT_PROCEED=0 / EXIT_FAIL=1 / EXIT_ESCALATE=2 / EXIT_RETRY=3`. `--agent` CLI arg takes the class identifier, not an agent name. `canonical_artifact_relpath` puts fan-out slugs (`foo--i1`) under the parent dir. `--normalize` flag lazy-imports `app.pipeline.normalize` and EXIT_RETRYs cleanly if 1.4 hasn't landed.

MEMORY[observation]: Pytest's default `python_functions = "test"` collects ANY function whose name starts with `test` (not just `test_`). Helper builders named like `test_header` or `tester_header` get scooped into the test suite and trigger `PytestReturnNotNoneWarning`. Prefix internal helpers with `_` or use a non-`test` verb.

STATUS: DONE
```
